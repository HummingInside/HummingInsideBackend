spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show-sql: true
        format_sql: true
        #generate-ddl: true
        use_sql_comments: true

logging.level:
  org.hibernate.SQL: debug
